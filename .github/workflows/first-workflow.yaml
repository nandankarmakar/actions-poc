name: First Workflow
on:
  workflow_dispatch:
environment: dev
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Run JS
        run: node hello.js
      - name: Show env variable
        run: echo ${{ vars.USER_NAME}}